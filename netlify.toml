[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — all paths serve index.html so React Router handles them
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
